---
import Layout from '../../../layouts/Layout.astro';
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import HubNav from '../../../components/HubNav.astro';
import Icon from '../../../components/Icon.astro';
import '../../../styles/hub-common.css';
---

<Layout
  title="Resources — Operator Hub — Testnet Portal"
  description="Testnet RPC, bootnodes, and operator docs for Boing Network."
>
  <article class="portal-page role-hub operators-hub hub-page hub-operator" data-hub="node_operator">
    <script is:inline src="/portal-session.js"></script>
    <Breadcrumbs items={[{ label: 'Testnet Portal', href: '/testnet' }, { label: 'Operator Hub', href: '/testnet/operators' }, { label: 'Resources' }]} />
    <div class="hub-page-main">
    <HubNav hub="operator" current="resources" />
    <section class="section fade-in-up">
      <h1>Resources</h1>
      <p class="muted">Links to run and operate a node on testnet.</p>
      <ul class="resources-list hub-list-animate">
        <li>
          <span class="resource-icon"><Icon name="network" size={22} /></span>
          <a href="/network/testnet">Join Testnet</a>
          <p class="desc">Quick start, bootnodes</p>
        </li>
        <li>
          <span class="resource-icon"><Icon name="node" size={22} /></span>
          <a href="/network/bootnodes">Bootnodes</a>
          <p class="desc">What they are and how to use</p>
        </li>
        <li>
          <span class="resource-icon"><Icon name="faucet" size={22} /></span>
          <a href="/network/faucet">Faucet</a>
          <p class="desc">Get testnet BOING</p>
        </li>
        <li>
          <span class="resource-icon"><Icon name="rocket" size={22} /></span>
          <a href="/pdfs/VIBEMINER-INTEGRATION.pdf" target="_blank" rel="noopener">VibeMiner (PDF)</a>
          <p class="desc">One-click mining/validator UI</p>
        </li>
        <li>
          <span class="resource-icon"><Icon name="document" size={22} /></span>
          <a href="/pdfs/RUNBOOK.pdf" target="_blank" rel="noopener">RUNBOOK (PDF)</a>
          <p class="desc">Node operations</p>
        </li>
      </ul>
    </section>
  </div>
  </article>
</Layout>

<script is:inline src="/portal-session.js"></script>
<script>
  (function () {
    var expectedRole = 'node_operator';
    var session = window.BoingPortalSession && window.BoingPortalSession.getSession();
    var authEl = document.getElementById('portal-auth');
    if (session && session.role !== expectedRole) {
      var path = window.BoingPortalSession ? window.BoingPortalSession.roleAppPath(session.role) : '/testnet';
      window.location.href = path;
      return;
    }
    function renderAuth() {
      if (!authEl) return;
      if (!session) {
        authEl.innerHTML = '<a href="/testnet/sign-in" class="auth-link">Sign in</a>';
        return;
      }
      var short = window.BoingPortalSession ? window.BoingPortalSession.shortAccount(session.account_id_hex) : session.account_id_hex;
      authEl.innerHTML = 'Signed in as <span class="account-short">' + short + '</span> · <button type="button" id="portal-signout" class="auth-signout">Logout</button>';
      var so = document.getElementById('portal-signout');
      if (so) so.addEventListener('click', function () {
        if (window.BoingPortalSession && window.BoingPortalSession.logout) window.BoingPortalSession.logout();
        else { try { localStorage.removeItem('boing_portal_session'); } catch(_){} window.location.href = '/'; }
      });
    }
    renderAuth();
  })();
</script>
