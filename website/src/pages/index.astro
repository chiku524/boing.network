---
import Layout from '../layouts/Layout.astro';
import GenesisBlockIntro from '../components/GenesisBlockIntro.astro';
---

<Layout
  title="Boing Network"
  description="Boing Network — Authentic. Decentralized. Optimal. Quality-Assured. Authentic L1 blockchain built from first principles. Swap, stake, and build on the Boing ecosystem."
  canonical="/"
>
  <GenesisBlockIntro durationMs={4000} />

  <!-- Hero -->
  <section class="hero" id="hero">
    <!-- Robot hero: extracted Boing robot (from hero image) with 3D motion over environment background -->
    <div class="hero-robot-wrap" aria-hidden="true">
      <div class="hero-robot-3d">
        <img src="/assets/mascot-excited.png" alt="" class="hero-robot-img" width="480" height="480" />
      </div>
    </div>
    <div class="hero-bg" aria-hidden="true">
      <div class="hero-orb hero-orb-1"></div>
      <div class="hero-orb hero-orb-2"></div>
      <div class="hero-orb hero-orb-3"></div>
      <div class="float-particle float-particle-1"></div>
      <div class="float-particle float-particle-2"></div>
      <div class="float-particle float-particle-3"></div>
      <div class="float-particle float-particle-4"></div>
      <div class="float-particle float-particle-5"></div>
      <div class="shooting-star shooting-star-1"><div class="shooting-star-line line-1"></div></div>
      <div class="shooting-star shooting-star-2"><div class="shooting-star-line line-2"></div></div>
      <div class="shooting-star shooting-star-3"><div class="shooting-star-line line-3"></div></div>
      <div class="shooting-star shooting-star-4"><div class="shooting-star-line line-4"></div></div>
      <div class="shooting-star shooting-star-5"><div class="shooting-star-line line-5"></div></div>
      <div class="shooting-star shooting-star-6"><div class="shooting-star-line line-6"></div></div>
      <div class="shooting-star shooting-star-7"><div class="shooting-star-line line-7"></div></div>
      <div class="shooting-star shooting-star-8"><div class="shooting-star-line line-8"></div></div>
    </div>
    <div class="hero-content">
      <span class="stage-badge" aria-hidden="true">Testnet</span>
      <img src="/assets/icon-only-transparent.png" alt="Boing Network" class="hero-logo-img" width="120" height="120" />
      <h1 class="hero-title gradient-text-hero animate-pulse-glow">Boing Network</h1>
      <p class="tagline gradient-text-hero">Authentic. Decentralized. Optimal. Quality-Assured.</p>
      <p class="hero-stage">We're in <strong>Testnet</strong> — join now, run a node, and help shape the network.</p>
      <div class="hero-highlights">
        <span class="hero-highlight"><span class="hl-icon">🔐</span> Security</span>
        <span class="hero-highlight"><span class="hl-icon">⚡</span> Scalability</span>
        <span class="hero-highlight"><span class="hl-icon">🌐</span> Decentralization</span>
        <span class="hero-highlight"><span class="hl-icon">✓</span> Authenticity</span>
      </div>
      <div class="cta">
        <a href="/testnet" class="btn btn-testnet">Open Dashboard</a>
        <a href="/network/testnet" class="btn btn-secondary">Join Testnet</a>
        <a href="/resources" class="btn btn-outline">Resources</a>
      </div>
    </div>
  </section>

  <!-- The Pillars of the Boing Network -->
  <section class="section section-alt" id="pillars">
    <h2 class="display-title section-reveal">The Pillars of the Boing Network</h2>
    <p class="section-lead section-reveal delay-1">Six foundational principles that define who we are.</p>
    <div class="pillars-grid">
      <div class="pillar-card section-reveal delay-2"><img src="/assets/pillar-security.png" alt="Security" class="pillar-img" width="200" height="280" /><h3>Security</h3><p>Safety and correctness first — always over speed.</p></div>
      <div class="pillar-card section-reveal delay-3"><img src="/assets/pillar-scalability.png" alt="Scalability" class="pillar-img" width="200" height="280" /><h3>Scalability</h3><p>High throughput without compromising the other pillars.</p></div>
      <div class="pillar-card section-reveal delay-4"><img src="/assets/pillar-decentralization.png" alt="Decentralization" class="pillar-img" width="200" height="280" /><h3>Decentralization</h3><p>Anyone can participate. No one can shut the door.</p></div>
      <div class="pillar-card section-reveal delay-5"><img src="/assets/pillar-authenticity.png" alt="Authenticity" class="pillar-img" width="200" height="280" /><h3>Authenticity</h3><p>One chain, one identity — authentic and independent.</p></div>
      <div class="pillar-card section-reveal delay-6"><img src="/assets/pillar-transparency.png" alt="Transparency" class="pillar-img" width="200" height="280" /><h3>Transparency</h3><p>100% open — in design, governance, and operations. Trust through verification.</p></div>
      <div class="pillar-card section-reveal delay-7"><img src="/assets/pillar-quality.png" alt="Quality Assurance" class="pillar-img" width="200" height="280" /><h3>True Quality Assurance</h3><p>Only assets that meet protocol-defined rules and security bar are allowed on-chain. Automation resolves known cases; leniency for meme culture; no maliciousness. Community QA pool only for genuinely unknown cases.</p></div>
    </div>
  </section>

  <!-- Tokenomics -->
  <section class="section section-alt" id="tokenomics">
    <h2 class="section-reveal">Tokenomics</h2>
    <p class="section-lead section-reveal delay-1">Sustainable value design — <strong>uncapped total supply</strong> with a <strong>set initial supply</strong> at mainnet launch. When circulating supply reaches the floor, new waves are minted to rebalance.</p>
    <div class="stats-grid">
      <div class="stat-card section-reveal delay-2"><span class="stat-value">Uncapped</span><span class="stat-label">Total Supply</span></div>
      <div class="stat-card section-reveal delay-3"><span class="stat-value">Set at launch</span><span class="stat-label">Initial Supply</span></div>
      <div class="stat-card section-reveal delay-4"><span class="stat-value">~2s</span><span class="stat-label">Block Time</span></div>
    </div>
    <div class="chart-card chart-fullwidth section-reveal delay-5">
      <h3>Initial Supply Allocation</h3>
      <p class="chart-intro">How the fixed initial supply (at mainnet launch) is allocated. Staking rewards are topped up by new waves when supply reaches the floor, so the initial allocation leans more toward treasury and community.</p>
      <div class="chart-wrapper chart-supply chart-pie-wrap">
        <svg viewBox="0 0 100 100" class="supply-pie-svg">
          <circle cx="50" cy="50" r="40" fill="none" class="stroke-teal" stroke-width="40" stroke-dasharray="138.2 113.1" stroke-dashoffset="0" transform="rotate(-90 50 50)" stroke-linecap="butt" />
          <circle cx="50" cy="50" r="40" fill="none" class="stroke-cyan" stroke-width="40" stroke-dasharray="75.4 175.9" stroke-dashoffset="-138.2" transform="rotate(-90 50 50)" stroke-linecap="butt" />
          <circle cx="50" cy="50" r="40" fill="none" class="stroke-success" stroke-width="40" stroke-dasharray="37.7 213.6" stroke-dashoffset="-213.6" transform="rotate(-90 50 50)" stroke-linecap="butt" />
        </svg>
      </div>
      <ul class="legend legend-inline">
        <li><span class="dot dot-teal"></span> Staking Rewards 55%</li>
        <li><span class="dot dot-cyan"></span> Treasury 30%</li>
        <li><span class="dot dot-success"></span> Community & Grants 15%</li>
      </ul>
      <p class="chart-note">Total supply is uncapped. Initial supply is fixed at mainnet launch; new waves mint when circulating supply reaches the floor.</p>
    </div>
    <div class="charts-row">
      <div class="chart-card section-reveal delay-6">
        <h3>Fee Allocation</h3>
        <div class="chart-wrapper chart-doughnut">
          <svg viewBox="0 0 100 100" class="doughnut-svg">
            <circle cx="50" cy="50" r="40" fill="none" class="stroke-teal" stroke-width="20" stroke-dasharray="201 50.3" stroke-dashoffset="0" transform="rotate(-90 50 50)" />
            <circle cx="50" cy="50" r="40" fill="none" class="stroke-cyan" stroke-width="20" stroke-dasharray="50.3 201" stroke-dashoffset="-201" transform="rotate(-90 50 50)" />
          </svg>
        </div>
        <ul class="legend">
          <li><span class="dot dot-teal"></span> Validators 80%</li>
          <li><span class="dot dot-cyan"></span> Treasury 20%</li>
        </ul>
      </div>
      <div class="chart-card section-reveal delay-7">
        <h3>Emission Schedule</h3>
        <div class="chart-wrapper chart-line">
          <svg viewBox="0 0 200 120" class="line-svg" preserveAspectRatio="none">
            <defs>
              <linearGradient id="emissionGrad" x1="0" y1="0" x2="0" y2="1">
                <stop offset="0%" stop-color="var(--accent-teal)" stop-opacity="0.3" />
                <stop offset="100%" stop-color="var(--accent-teal)" stop-opacity="0" />
              </linearGradient>
            </defs>
            <polyline points="0,24 33,48 67,66 100,79 133,90 200,108 200,120 0,120" fill="url(#emissionGrad)" stroke="none" />
            <polyline points="0,24 33,48 67,66 100,79 133,90 200,108" fill="none" class="stroke-teal" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </div>
      </div>
    </div>
    <p class="value-loop section-reveal delay-8"><strong>Usage ↑</strong> → Fees ↑ → Validators & Treasury ↑ → Security & Ecosystem ↑ → <strong>Value ↑</strong></p>
  </section>

  <!-- Roadmap & Ecosystem (merged) -->
  <section class="section section-alt" id="roadmap">
    <h2 class="section-reveal">Roadmap</h2>
    <p class="section-lead section-reveal delay-1">Phased build from first principles.</p>
    <div class="timeline-compact">
      <div class="t-item complete section-reveal delay-2"><span class="t-marker">✓</span><span>Foundation</span></div>
      <div class="t-item complete section-reveal delay-3"><span class="t-marker">✓</span><span>Networking</span></div>
      <div class="t-item complete section-reveal delay-4"><span class="t-marker">✓</span><span>Verkle & VM</span></div>
      <div class="t-item in-progress section-reveal delay-5"><span class="t-marker">●</span><span>PoS & Production</span></div>
      <div class="t-item in-progress section-reveal delay-6"><span class="t-marker">●</span><span>UX & Automation</span></div>
      <div class="t-item planned section-reveal delay-7"><span class="t-marker">○</span><span>Cross-chain</span></div>
      <div class="t-item planned section-reveal delay-8"><span class="t-marker">○</span><span>Testnet & Mainnet</span></div>
    </div>
    <h2 class="section-subtitle section-reveal delay-9">Ecosystem</h2>
    <p class="section-lead section-reveal delay-10">Applications building on Boing Network.</p>
    <div class="apps-row">
      <a href="https://boing.finance" class="app-card featured section-reveal delay-11" target="_blank" rel="noopener">
        <h3>boing.finance</h3>
        <p>Swap, bridge, deploy tokens, manage liquidity across EVM & Solana.</p>
        <span class="app-status live">Live</span>
      </a>
      <a href="/docs/getting-started" class="app-card section-reveal delay-12">
        <h3>Boing Network</h3>
        <p>Run a node, stake, participate in governance. The foundation layer.</p>
        <span class="app-status live">Live</span>
      </a>
      <a href="/developers/quickstart" class="app-card section-reveal delay-13">
        <h3>Build on Boing</h3>
        <p>Native AA, adaptive gas, success-based incentives for developers.</p>
        <span class="app-status build">Build</span>
      </a>
    </div>
  </section>

  <!-- Build & Resources (merged: innovations, testnet CTA, CTA banner, resources) -->
  <section class="section" id="build">
    <h2 class="section-reveal">Unique Innovations</h2>
    <p class="section-lead section-reveal delay-1">Protocol-level features that set Boing apart.</p>
    <div class="grid">
      <div class="card section-reveal delay-2"><h3>Native Account Abstraction</h3><p>Gasless txs, social recovery, session keys — built into the protocol.</p></div>
      <div class="card section-reveal delay-3"><h3>Adaptive Gas Model</h3><p>Dynamic gas that responds to network conditions with predictable caps.</p></div>
      <div class="card section-reveal delay-4"><h3>Cross-Chain DeFi Hub</h3><p>Protocol-level primitives for liquidity routing across chains.</p></div>
      <div class="card section-reveal delay-5"><h3>Transparent Slashing</h3><p>Auditable slashing conditions and appeal mechanisms.</p></div>
    </div>
    <div class="build-banners-row">
      <div class="testnet-banner section-reveal delay-6">
        <h3>🌐 Join the Testnet</h3>
        <p>Run a node, get testnet BOING from the faucet, and help secure the network. Single node for local dev or multi-node to join the public testnet.</p>
        <div class="testnet-banner-btns">
          <a href="/network/testnet" class="btn btn-testnet">Join Testnet</a>
          <a href="/network/faucet" class="btn btn-secondary">Get testnet BOING</a>
        </div>
      </div>
      <div class="cta-banner section-reveal delay-7">
        <h3>🚀 Build on Boing Network</h3>
        <p>Authentic L1 blockchain built from first principles. Native AA, adaptive gas, cross-chain DeFi hub.</p>
        <div class="cta-banner-links">
          <a href="/network/testnet">Join Testnet</a>
          <span class="sep">•</span>
          <a href="/docs/getting-started">Get Started</a>
          <span class="sep">•</span>
          <a href="/developers/quickstart">Developers</a>
          <span class="sep">•</span>
          <a href="https://boing.finance" target="_blank" rel="noopener">boing.finance</a>
        </div>
      </div>
    </div>
    <h2 class="section-subtitle section-reveal delay-8">Resources</h2>
    <div class="link-grid">
      <a href="/testnet" class="link-highlight link-3d">Dashboard</a>
      <a href="/resources" class="link-highlight link-3d">Resources</a>
      <a href="/network/testnet" class="link-highlight link-3d">Join Testnet</a>
      <a href="/network/faucet" class="link-3d">Faucet</a>
      <a href="/docs/getting-started" class="link-3d">Getting Started</a>
      <a href="/docs/rpc-api" class="link-3d">RPC API</a>
      <a href="/developers/quickstart" class="link-3d">Developer Quickstart</a>
      <a href="/network/status" class="link-3d">Network Status</a>
      <a href="/about" class="link-3d">About</a>
      <a href="/community" class="link-3d">Community</a>
    </div>
    <footer class="section-footer">
      <p>Boing Network — Authentic. Decentralized. Optimal. Quality-Assured. <strong>Currently in Testnet.</strong></p>
      <div class="footer-links">
        <a href="/testnet">Dashboard</a>
        <a href="/resources">Resources</a>
        <a href="/network/testnet">Join Testnet</a>
        <a href="/network/faucet">Faucet</a>
        <a href="https://github.com/chiku524/boing.network" target="_blank" rel="noopener">GitHub</a>
        <a href="https://boing.finance" target="_blank" rel="noopener">boing.finance</a>
        <a href="/docs">Documentation</a>
      </div>
    </footer>
  </section>
</Layout>

<style>
  /* Section-by-section scroll snap (homepage only). scroll-margin-top for fixed nav. */
  :global(html.page-home) { scroll-snap-type: y mandatory; scroll-behavior: smooth; }
  :global(html.page-home) .hero,
  :global(html.page-home) .section { scroll-snap-align: start; scroll-snap-stop: always; min-height: 100vh; scroll-margin-top: 4.5rem; }

  /* Hero + animation */
  .hero { position: relative; text-align: center; padding: 5rem 2rem; overflow: hidden; display: grid; grid-template-columns: 1fr; grid-template-rows: auto auto; align-items: center; justify-items: center; min-height: 85vh; gap: 1rem; }
  @media (max-width: 480px) {
    .hero { padding: 3rem 1rem; min-height: 80vh; }
  }
  .hero-content { order: 1; }
  .hero-robot-wrap { order: 2; }
  @media (min-width: 900px) {
    .hero { grid-template-columns: 1fr auto; grid-template-rows: 1fr; gap: 2rem; text-align: left; }
    .hero-content { order: unset; margin: 0; max-width: 520px; justify-self: end; }
    .hero-robot-wrap { order: unset; }
    .hero-highlights { justify-content: flex-start; }
    .cta { justify-content: flex-start; }
  }
  .hero-robot-wrap {
    position: relative;
    display: flex; align-items: center; justify-content: center;
    pointer-events: none; z-index: 2;
    perspective: 1400px;
    transition: opacity 0.9s cubic-bezier(0.22, 1, 0.36, 1), transform 0.9s cubic-bezier(0.22, 1, 0.36, 1);
  }
  :global(html.intro-active) .hero-robot-wrap { opacity: 0; transform: scale(0.96); }
  :global(html.intro-complete) .hero-robot-wrap { opacity: 1; transform: scale(1); }
  @media (min-width: 900px) {
    .hero-robot-wrap { position: absolute; right: 0; bottom: 0; top: 0; left: 45%; }
  }
  .hero-robot-3d {
    transform-style: preserve-3d;
    animation: hero-robot-float 7s ease-in-out infinite;
    filter: drop-shadow(0 0 40px rgba(0, 229, 204, 0.2)) drop-shadow(0 20px 40px rgba(0, 0, 0, 0.3));
  }
  .hero-robot-wrap .hero-robot-3d {
    position: relative;
    width: min(55vw, 420px);
    height: min(55vw, 420px);
    max-width: 480px;
    max-height: 75vh;
  }
  @media (min-width: 900px) {
    .hero-robot-wrap .hero-robot-3d { width: min(42vw, 480px); height: min(42vw, 480px); }
  }
  .hero-robot-img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center bottom;
    animation: hero-robot-glow 4s ease-in-out infinite;
  }
  @keyframes hero-robot-float {
    0%, 100% { transform: translateY(0) rotateY(-6deg) rotateX(3deg); }
    50% { transform: translateY(-14px) rotateY(4deg) rotateX(-2deg); }
  }
  @keyframes hero-robot-glow {
    0%, 100% { filter: drop-shadow(0 0 40px rgba(0, 229, 204, 0.2)); opacity: 1; }
    50% { filter: drop-shadow(0 0 56px rgba(0, 229, 204, 0.35)); opacity: 0.98; }
  }
  .hero-bg {
    position: absolute; inset: 0; pointer-events: none; z-index: 0;
    transition: opacity 1s cubic-bezier(0.22, 1, 0.36, 1);
  }
  :global(html.intro-active) .hero-bg { opacity: 0; }
  :global(html.intro-complete) .hero-bg { opacity: 1; }
  .hero-orb { position: absolute; border-radius: 50%; filter: blur(80px); opacity: 0.4; animation: float 20s ease-in-out infinite; }
  .hero-orb-1 { width: 400px; height: 400px; background: radial-gradient(circle, var(--accent-teal) 0%, transparent 70%); top: -20%; left: 10%; animation-delay: 0s; }
  .hero-orb-2 { width: 300px; height: 300px; background: radial-gradient(circle, var(--accent-cyan) 0%, transparent 70%); bottom: -10%; right: 15%; animation-delay: -7s; }
  .hero-orb-3 { width: 250px; height: 250px; background: radial-gradient(circle, var(--accent-teal) 0%, transparent 70%); top: 30%; right: 5%; animation-delay: -14s; }
  @keyframes float { 0%, 100% { transform: translate(0, 0) scale(1); } 33% { transform: translate(30px, -20px) scale(1.05); } 66% { transform: translate(-20px, 15px) scale(0.95); } }
  /* Shooting stars — gradient lines inspired by boing.finance */
  .shooting-star { position: absolute; width: 0; height: 0; overflow: visible; }
  .shooting-star-line {
    position: absolute; width: 128px; height: 2px; left: 0; top: 0;
    border-radius: 1px;
    transform-origin: left center;
  }
  /* Each line rotation matches keyframe movement. Angles: 0°=right, 90°=down, 180°=left. vw units preserve angle. */
  .line-1 { background: linear-gradient(to right, transparent, var(--glow-cyan), var(--accent-teal)); box-shadow: 0 0 8px var(--glow-cyan); transform: rotate(-16deg); }
  .line-2 { background: linear-gradient(to left, transparent, var(--glow-blue), var(--accent-cyan)); box-shadow: 0 0 8px var(--glow-blue); transform-origin: right center; transform: rotate(-55deg); }
  .line-3 { background: linear-gradient(to right, transparent, var(--glow-cyan), var(--accent-cyan)); box-shadow: 0 0 8px var(--glow-cyan); transform: rotate(45deg); }
  .line-4 { background: linear-gradient(to left, transparent, var(--glow-cyan), var(--accent-teal)); box-shadow: 0 0 6px var(--glow-cyan-subtle); transform-origin: right center; transform: rotate(45deg); }
  .line-5 { background: linear-gradient(to right, transparent, var(--glow-cyan), var(--accent-teal)); box-shadow: 0 0 6px var(--glow-cyan-subtle); transform: rotate(-10deg); }
  .line-6 { background: linear-gradient(to left, transparent, var(--glow-blue), var(--accent-cyan)); box-shadow: 0 0 6px var(--glow-blue); transform-origin: right center; transform: rotate(-17deg); }
  .line-7 { background: linear-gradient(to right, transparent, var(--glow-cyan), var(--accent-cyan)); box-shadow: 0 0 6px var(--glow-cyan-subtle); transform: rotate(35deg); }
  .line-8 { background: linear-gradient(to left, transparent, var(--glow-cyan), var(--accent-teal)); box-shadow: 0 0 6px var(--glow-cyan-subtle); transform-origin: right center; transform: rotate(45deg); }
  .shooting-star-1 { top: 25%; left: 0; animation: shoot1 8s linear infinite; opacity: 0.6; }
  .shooting-star-2 { top: 33%; right: 0; animation: shoot2 12s linear infinite; animation-delay: -2s; opacity: 0.5; }
  .shooting-star-3 { bottom: 25%; left: 25%; animation: shoot3 10s linear infinite; animation-delay: -4s; opacity: 0.4; }
  .shooting-star-4 { top: 50%; right: 33%; animation: shoot4 15s linear infinite; animation-delay: -7s; opacity: 0.3; }
  .shooting-star-5 { top: 17%; right: 25%; animation: shoot5 6s linear infinite; animation-delay: -1s; opacity: 0.5; }
  .shooting-star-6 { bottom: 33%; left: 33%; animation: shoot6 9s linear infinite; animation-delay: -5s; opacity: 0.45; }
  .shooting-star-7 { top: 40%; left: 17%; animation: shoot7 11s linear infinite; animation-delay: -6s; opacity: 0.4; }
  .shooting-star-8 { top: 75%; right: 17%; animation: shoot8 14s linear infinite; animation-delay: -9s; opacity: 0.35; }
  .float-particle { position: absolute; border-radius: 50%; background: linear-gradient(to right, var(--accent-teal), var(--accent-cyan)); box-shadow: 0 0 6px var(--glow-cyan); animation: floatSlow 6s ease-in-out infinite, pulseFade 3s ease-in-out infinite; }
  .float-particle-1 { width: 4px; height: 4px; top: 15%; left: 20%; animation-delay: 0s, 0s; }
  .float-particle-2 { width: 2px; height: 2px; top: 65%; right: 25%; animation-delay: -2s, -0.5s; }
  .float-particle-3 { width: 2px; height: 2px; bottom: 30%; left: 50%; animation-delay: -4s, -1s; }
  .float-particle-4 { width: 3px; height: 3px; top: 40%; left: 25%; background: linear-gradient(to right, var(--accent-cyan), var(--accent-teal)); box-shadow: 0 0 6px var(--glow-blue); animation-delay: -1s, -0.3s; }
  .float-particle-5 { width: 2px; height: 2px; top: 75%; right: 15%; animation-delay: -3s, -0.8s; }
  /* Movement (dx,dy) uses vw so angle stays consistent. dx:dy ratio = tan(angle). */
  @keyframes shoot1 { 0% { transform: translate(0, 0); opacity: 0; } 10% { opacity: 0.6; } 90% { opacity: 0.6; } 100% { transform: translate(100vw, -28vw); opacity: 0; } } /* -16° */
  @keyframes shoot2 { 0% { transform: translate(0, 0); opacity: 0; } 10% { opacity: 0.5; } 90% { opacity: 0.5; } 100% { transform: translate(-100vw, 143vw); opacity: 0; } } /* 125° dy/dx=tan(125°)=1.43 */
  @keyframes shoot3 { 0% { transform: translate(0, 0); opacity: 0; } 10% { opacity: 0.4; } 90% { opacity: 0.4; } 100% { transform: translate(100vw, 100vw); opacity: 0; } } /* 45° */
  @keyframes shoot4 { 0% { transform: translate(0, 0); opacity: 0; } 10% { opacity: 0.3; } 90% { opacity: 0.3; } 100% { transform: translate(-80vw, -80vw); opacity: 0; } } /* 225° */
  @keyframes shoot5 { 0% { transform: translate(0, 0); opacity: 0; } 5% { opacity: 0.5; } 95% { opacity: 0.5; } 100% { transform: translate(100vw, -18vw); opacity: 0; } } /* -10° */
  @keyframes shoot6 { 0% { transform: translate(0, 0); opacity: 0; } 8% { opacity: 0.45; } 92% { opacity: 0.45; } 100% { transform: translate(-90vw, 27vw); opacity: 0; } } /* 163° tan≈0.3 */
  @keyframes shoot7 { 0% { transform: translate(0, 0); opacity: 0; } 12% { opacity: 0.4; } 88% { opacity: 0.4; } 100% { transform: translate(100vw, 70vw); opacity: 0; } } /* 35° */
  @keyframes shoot8 { 0% { transform: translate(0, 0); opacity: 0; } 7% { opacity: 0.35; } 93% { opacity: 0.35; } 100% { transform: translate(-60vw, -60vw); opacity: 0; } } /* 225° */
  .hero-content {
    position: relative;
    z-index: 3;
    max-width: 800px;
    margin: 0 auto;
  }
  :global(html.intro-active) .hero-content {
    pointer-events: none;
  }
  :global(html.intro-active) .hero-content .hero-logo-img,
  :global(html.intro-active) .hero-content .hero-title,
  :global(html.intro-active) .hero-content .tagline,
  :global(html.intro-active) .hero-content .stage-badge,
  :global(html.intro-active) .hero-content .hero-stage,
  :global(html.intro-active) .hero-content .hero-highlights,
  :global(html.intro-active) .hero-content .cta {
    opacity: 0;
    transform: translateY(24px) scale(0.98);
  }
  :global(html.intro-complete) .hero-content .hero-logo-img,
  :global(html.intro-complete) .hero-content .hero-title,
  :global(html.intro-complete) .hero-content .tagline,
  :global(html.intro-complete) .hero-content .stage-badge,
  :global(html.intro-complete) .hero-content .hero-stage,
  :global(html.intro-complete) .hero-content .hero-highlights,
  :global(html.intro-complete) .hero-content .cta {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
  .hero-content .hero-logo-img,
  .hero-content .hero-title,
  .hero-content .tagline,
  .hero-content .stage-badge,
  .hero-content .hero-stage,
  .hero-content .hero-highlights,
  .hero-content .cta {
    transition: opacity 0.85s cubic-bezier(0.22, 1, 0.36, 1), transform 0.85s cubic-bezier(0.22, 1, 0.36, 1);
  }
  :global(html.intro-complete) .hero-content .hero-logo-img { transition-delay: 0.02s; }
  :global(html.intro-complete) .hero-content .hero-title { transition-delay: 0.1s; }
  :global(html.intro-complete) .hero-content .tagline { transition-delay: 0.18s; }
  :global(html.intro-complete) .hero-content .stage-badge { transition-delay: 0.05s; }
  :global(html.intro-complete) .hero-content .hero-stage { transition-delay: 0.14s; }
  :global(html.intro-complete) .hero-content .hero-highlights { transition-delay: 0.26s; }
  :global(html.intro-complete) .hero-content .cta { transition-delay: 0.34s; }
  .hero-logo-img { display: block; max-width: min(120px, 28vw); height: auto; margin: 0 auto 0.5rem; object-fit: contain; filter: drop-shadow(0 0 16px var(--glow-cyan)); }
  .hero-title { font-size: clamp(2rem, 5vw, 3.5rem); font-weight: 700; margin: 0 0 0.5rem; background: linear-gradient(to right, var(--accent-teal), var(--accent-cyan)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; filter: drop-shadow(0 0 20px var(--glow-cyan)); }
  .tagline { font-size: 1.25rem; margin: 0 0 0.5rem; background: linear-gradient(to right, var(--accent-teal), var(--accent-cyan)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; filter: drop-shadow(0 0 12px var(--glow-cyan)); }
  .stage-badge {
    display: inline-block;
    padding: 0.35rem 0.85rem;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #f59e0b;
    background: rgba(245, 158, 11, 0.15);
    border: 1px solid rgba(245, 158, 11, 0.4);
    border-radius: 999px;
    margin-bottom: 0.75rem;
  }
  .hero-stage {
    font-size: 1rem;
    color: var(--text-secondary);
    margin: 0 0 1rem;
  }
  .hero-stage strong { color: #f59e0b; }
  .hero-highlights { display: flex; flex-wrap: wrap; gap: 0.5rem 1rem; justify-content: center; margin: 1.5rem 0 2rem; }
  .hero-highlight { display: inline-flex; align-items: center; gap: 0.35rem; padding: 0.4rem 0.75rem; border-radius: 0.5rem; font-size: 0.9rem; color: var(--text-secondary); background: var(--bg-card); border: 1px solid var(--border-color); transition: all 0.3s; }
  .hero-highlight:hover { border-color: var(--border-hover); transform: scale(1.05); box-shadow: 0 4px 16px var(--glow-cyan); color: var(--text-primary); }
  .hl-icon { font-size: 1rem; }
  .cta { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
  .btn { padding: 0.75rem 1.5rem; border-radius: 0.5rem; font-weight: 600; text-decoration: none; transition: all 0.2s; }
  .btn-primary { background: linear-gradient(to right, var(--primary-color), var(--secondary-color)); color: white; border: none; }
  .btn-primary:hover { filter: brightness(1.1); }
  .btn-secondary { background: var(--bg-tertiary); color: var(--text-primary); border: 1px solid var(--border-color); }
  .btn-secondary:hover { border-color: var(--accent-neon); color: var(--accent-neon); }
  .btn-outline { background: transparent; color: var(--text-secondary); border: 1px solid var(--border-color); }
  .btn-outline:hover { border-color: var(--accent-neon); color: var(--accent-neon); }

  /* Sections: full viewport width with inner max-width for readability */
  .section {
    position: relative;
    z-index: 1;
    padding: 4rem 2rem;
    max-width: 100%;
    width: 100%;
    box-sizing: border-box;
  }
  .section > * {
    max-width: 1280px;
    margin-left: auto;
    margin-right: auto;
  }
  .section-alt { background: transparent; }
  .section h2 { text-align: center; margin-bottom: 0.5rem; color: var(--text-primary); }
  .section h2.section-subtitle { margin-top: 3rem; font-size: 1.35rem; }
  .section-lead { text-align: center; color: var(--text-secondary); margin-bottom: 2rem; font-size: 1rem; }
  #build .section-subtitle { margin-top: 2.5rem; }

  .build-banners-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
    margin-top: 2rem;
    align-items: start;
  }
  @media (min-width: 720px) {
    .build-banners-row { grid-template-columns: 1fr 1fr; gap: 1.5rem; }
  }
  .build-banners-row .testnet-banner,
  .build-banners-row .cta-banner { margin: 0; }

  .section-footer {
    margin-top: 2.5rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border-color);
    text-align: center;
    color: var(--text-tertiary);
    font-size: 0.875rem;
  }
  .section-footer .footer-links {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 0.5rem;
    flex-wrap: wrap;
  }
  .section-footer a { color: var(--accent-neon); }

  /* Pillars */
  .pillars-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 1.5rem; margin-top: 1rem; }
  .pillar-card { background: var(--bg-card); border: 1px solid var(--border-color); border-radius: 16px; padding: 1rem; text-align: center; transition: border-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease; backdrop-filter: blur(12px); }
  .pillar-card:hover { border-color: var(--border-hover); box-shadow: 0 4px 30px var(--glow-cyan); transform: translateY(-2px); }
  .pillar-img { width: 100%; height: auto; max-height: 220px; object-fit: contain; border-radius: 8px; display: block; margin: 0 auto 0.75rem; }
  .pillar-card h3 { font-size: 1rem; margin: 0 0 0.35rem; color: var(--text-primary); }
  .pillar-card p { font-size: 0.8rem; color: var(--text-secondary); margin: 0; line-height: 1.4; }

  /* Section reveal: stronger entrance with stagger */
  .section-reveal { opacity: 0; transform: translateY(28px); animation: section-reveal 0.7s cubic-bezier(0.22, 1, 0.36, 1) forwards; }
  .section-reveal.delay-1 { animation-delay: 0.06s; }
  .section-reveal.delay-2 { animation-delay: 0.12s; }
  .section-reveal.delay-3 { animation-delay: 0.18s; }
  .section-reveal.delay-4 { animation-delay: 0.24s; }
  .section-reveal.delay-5 { animation-delay: 0.3s; }
  .section-reveal.delay-6 { animation-delay: 0.36s; }
  .section-reveal.delay-7 { animation-delay: 0.42s; }
  .section-reveal.delay-8 { animation-delay: 0.48s; }
  .section-reveal.delay-9 { animation-delay: 0.54s; }
  .section-reveal.delay-10 { animation-delay: 0.6s; }
  .section-reveal.delay-11 { animation-delay: 0.66s; }
  .section-reveal.delay-12 { animation-delay: 0.72s; }
  .section-reveal.delay-13 { animation-delay: 0.78s; }
  @keyframes section-reveal { to { opacity: 1; transform: translateY(0); } }
  @media (prefers-reduced-motion: reduce) { .section-reveal { animation: none; opacity: 1; transform: none; } .pillar-card:hover { transform: none; } }

  /* Sections: full viewport width with inner max-width for readability */
  .section {
    position: relative;
    z-index: 1;
    padding: 4rem 2rem;
    max-width: 100%;
    width: 100%;
    box-sizing: border-box;
  }
  .section > * {
    max-width: 1280px;
    margin-left: auto;
    margin-right: auto;
  }
  .section-alt { background: transparent; }
  .section h2 { text-align: center; margin-bottom: 0.5rem; color: var(--text-primary); }
  .section h2.section-subtitle { margin-top: 3rem; font-size: 1.35rem; }
  .section-lead { text-align: center; color: var(--text-secondary); margin-bottom: 2rem; font-size: 1rem; }
  #build .section-subtitle { margin-top: 2.5rem; }

  /* Tokenomics */
  .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin-bottom: 2rem; }
  .stat-card { background: var(--bg-card); border: 1px solid var(--border-color); border-radius: 1rem; padding: 1.25rem; text-align: center; transition: transform 0.3s ease, box-shadow 0.3s, border-color 0.3s; backdrop-filter: blur(12px); }
  .stat-card:hover { box-shadow: 0 8px 24px var(--glow-cyan); border-color: var(--border-hover); }
  .stat-value { display: block; font-size: 1.5rem; font-weight: 700; color: var(--accent-teal); }
  .stat-label { font-size: 0.85rem; color: var(--text-secondary); }
  .chart-fullwidth { margin-bottom: 1.5rem; }
  .chart-wrapper.chart-supply { height: 220px; min-height: 220px; display: flex; align-items: center; justify-content: center; }
  .chart-supply .supply-pie-svg { width: 100%; height: 100%; max-width: 220px; max-height: 220px; display: block; margin: 0 auto; }
  .chart-intro { font-size: 0.875rem; color: var(--text-secondary); margin: 0 0 0.75rem; line-height: 1.45; }
  .chart-note { font-size: 0.8rem; color: var(--text-tertiary); margin: 0.5rem 0 0; font-style: italic; }
  .legend-inline { justify-content: flex-start; margin-top: 0.75rem; }
  .charts-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; margin-bottom: 1.5rem; }
  .chart-card { background: var(--bg-card); border: 1px solid var(--border-color); border-radius: 1rem; padding: 1.25rem; backdrop-filter: blur(12px); transition: border-color 0.3s, box-shadow 0.3s; }
  .chart-card:hover { border-color: var(--border-hover); box-shadow: 0 8px 24px var(--glow-cyan); }
  .chart-card h3 { font-size: 1rem; margin: 0 0 0.75rem; color: var(--text-primary); }
  .chart-wrapper { position: relative; width: 100%; height: 220px; min-height: 220px; }
  .chart-doughnut .doughnut-svg { width: 100%; height: 100%; max-width: 200px; max-height: 200px; margin: 0 auto; display: block; }
  .chart-line .line-svg { width: 100%; height: 100%; display: block; }
  .legend { list-style: none; padding: 0; margin: 0.5rem 0 0; display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; font-size: 0.85rem; color: var(--text-secondary); }
  .legend .dot { display: inline-block; width: 8px; height: 8px; border-radius: 50%; margin-right: 0.25rem; vertical-align: middle; }
  .dot-teal { background: var(--accent-teal); }
  .dot-cyan { background: var(--accent-cyan); }
  .dot-success { background: var(--success-color); }
  .stroke-teal { stroke: var(--accent-teal); }
  .stroke-cyan { stroke: var(--accent-cyan); }
  .stroke-success { stroke: var(--success-color); }
  .value-loop { text-align: center; font-family: var(--font-mono); color: var(--accent-teal); background: var(--bg-tertiary); border: 1px solid var(--border-color); padding: 1rem; border-radius: 0.5rem; font-size: 0.95rem; }

  .t-item { display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem 1rem; background: var(--bg-card); border: 1px solid var(--border-color); border-radius: 0.5rem; font-size: 0.9rem; color: var(--text-secondary); transition: transform 0.2s, border-color 0.2s; backdrop-filter: blur(8px); }
  .t-item.complete { border-color: rgba(16,185,129,0.4); color: var(--success-color); }
  .t-item.in-progress { border-color: var(--accent-teal); color: var(--accent-teal); }
  .t-marker { font-size: 0.75rem; }

  /* Ecosystem */
  .apps-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 1rem; }
  .app-card { background: var(--bg-card); border: 1px solid var(--border-color); border-radius: 1rem; padding: 1.25rem; text-decoration: none; color: inherit; transition: transform 0.3s ease, border-color 0.3s, box-shadow 0.3s; backdrop-filter: blur(12px); }
  .app-card:hover { border-color: var(--border-hover); box-shadow: 0 12px 32px var(--glow-cyan); }
  .app-card.featured { border-color: var(--accent-cyan); }
  .app-card h3 { font-size: 1rem; margin: 0 0 0.5rem; color: var(--text-primary); }
  .app-card p { font-size: 0.875rem; color: var(--text-secondary); margin: 0 0 0.75rem; line-height: 1.4; }
  .app-status { font-size: 0.75rem; text-transform: uppercase; font-weight: 600; }
  .app-status.live { color: var(--success-color); }
  .app-status.build { color: var(--accent-teal); }

  /* Innovations grid */
  .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; }
  .card { padding: 1.25rem; background: var(--bg-card); border: 1px solid var(--border-color); border-radius: 1rem; transition: transform 0.3s ease, border-color 0.3s, box-shadow 0.3s; backdrop-filter: blur(12px); }
  .card:hover { border-color: var(--border-hover); box-shadow: 0 10px 28px var(--glow-cyan); }
  .card h3 { font-size: 0.95rem; margin: 0 0 0.5rem; color: var(--text-primary); }
  .card p { margin: 0; font-size: 0.85rem; color: var(--text-secondary); }

  /* Testnet CTA banner */
  .testnet-banner { padding: 1.75rem 2rem; border-radius: 1rem; background: var(--bg-card); border: 1px solid var(--border-hover); text-align: center; backdrop-filter: blur(8px); }
  .testnet-banner h3 { margin: 0 0 0.5rem; font-size: 1.2rem; color: var(--text-primary); }
  .testnet-banner p { margin: 0 0 1.25rem; font-size: 0.95rem; color: var(--text-secondary); line-height: 1.45; }
  .testnet-banner-btns { display: flex; flex-wrap: wrap; gap: 0.75rem; justify-content: center; }
  .testnet-banner-btns .btn { padding: 0.6rem 1.25rem; font-size: 0.95rem; }

  /* CTA Banner */
  .cta-banner { padding: 1.5rem 2rem; border-radius: 1rem; background: var(--bg-card); border: 1px solid var(--border-color); text-align: center; backdrop-filter: blur(8px); }
  .cta-banner h3 { margin: 0 0 0.5rem; font-size: 1.1rem; color: var(--text-primary); }
  .cta-banner p { margin: 0 0 1rem; font-size: 0.9rem; color: var(--text-secondary); }
  .cta-banner-links { display: flex; flex-wrap: wrap; gap: 0.5rem; justify-content: center; align-items: center; font-size: 0.9rem; }
  .cta-banner-links a { color: var(--accent-teal); text-decoration: underline; }
  .cta-banner-links a:hover { color: var(--accent-cyan); }
  .cta-banner-links .sep { color: var(--text-tertiary); margin: 0 0.25rem; }

  /* Links */
  .link-grid { display: flex; gap: 1rem; flex-wrap: wrap; justify-content: center; }
  .link-grid a { padding: 0.5rem 1rem; border-radius: 0.5rem; background: var(--bg-card); border: 1px solid var(--border-color); transition: all 0.3s; }
  .link-grid a:hover { border-color: var(--border-hover); color: var(--accent-teal); box-shadow: 0 4px 12px var(--glow-cyan); }
  .link-grid a.link-highlight { border-color: var(--border-hover); background: var(--bg-tertiary); font-weight: 600; }
  .link-grid a.link-highlight:hover { border-color: var(--accent-teal); box-shadow: 0 0 12px var(--glow-cyan); }

  @media (max-width: 640px) { .stats-grid { grid-template-columns: 1fr; } }
  .t-item { transition: transform 0.2s ease; }
  .t-item:hover { transform: scale(1.02); }
  @media (prefers-reduced-motion: reduce) {
    .hero-orb, .shooting-star, .float-particle { animation: none; }
    .hero-orb { opacity: 0.25; }
    .shooting-star, .float-particle { display: none; }
    .hero-robot-3d { animation: none; transform: translateY(0) rotateY(-4deg) rotateX(2deg); }
    .hero-robot-img { animation: none; }
    .card:hover, .app-card:hover, .stat-card:hover, .t-item:hover { transform: none; }
  }
</style>

<script>
  /** Homepage scroll: section-by-section, but scroll within tall sections first so all content is viewable. */
  (() => {
    if (!document.documentElement.classList.contains('page-home')) return;
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    const sections = Array.from(document.querySelectorAll('.hero, .section'));
    if (sections.length < 2) return;

    let cooldown = false;
    const COOLDOWN_MS = 500;
    const NAV_OFFSET = 4.5 * 16; // match scroll-margin-top (72px)
    const THRESHOLD = 40;
    const STEP = 0.75; // fraction of viewport to scroll within section

    function findCurrentIndex() {
      const mid = window.innerHeight * 0.4;
      for (let i = sections.length - 1; i >= 0; i--) {
        if (sections[i].getBoundingClientRect().top <= mid) return i;
      }
      return 0;
    }

    function goToSection(index) {
      if (index < 0 || index >= sections.length) return;
      sections[index].scrollIntoView({ behavior: 'smooth', block: 'start' });
    }

    function atTopOfSection(section) {
      const rect = section.getBoundingClientRect();
      return rect.top >= NAV_OFFSET - THRESHOLD;
    }

    function atBottomOfSection(section) {
      const rect = section.getBoundingClientRect();
      return rect.bottom <= window.innerHeight + THRESHOLD;
    }

    window.addEventListener('wheel', (e) => {
      if (cooldown) { e.preventDefault(); return; }
      const delta = e.deltaY;
      if (Math.abs(delta) < 20) return;

      const i = findCurrentIndex();
      const section = sections[i];
      const rect = section.getBoundingClientRect();
      const sectionTallerThanView = rect.height > window.innerHeight;

      e.preventDefault();

      if (delta > 0) {
        if (sectionTallerThanView && !atBottomOfSection(section)) {
          const step = Math.min(window.innerHeight * STEP, rect.bottom - window.innerHeight - 20);
          if (step > 10) {
            window.scrollBy({ top: step, behavior: 'smooth' });
            cooldown = true;
            setTimeout(() => { cooldown = false; }, COOLDOWN_MS);
            return;
          }
        }
        goToSection(Math.min(i + 1, sections.length - 1));
      } else {
        if (sectionTallerThanView && !atTopOfSection(section)) {
          const step = Math.min(window.innerHeight * STEP, rect.top - NAV_OFFSET + 20);
          if (step > 10) {
            window.scrollBy({ top: -step, behavior: 'smooth' });
            cooldown = true;
            setTimeout(() => { cooldown = false; }, COOLDOWN_MS);
            return;
          }
        }
        goToSection(Math.max(i - 1, 0));
      }

      cooldown = true;
      setTimeout(() => { cooldown = false; }, COOLDOWN_MS);
    }, { passive: false });

    window.addEventListener('keydown', (e) => {
      if (e.key !== 'ArrowDown' && e.key !== 'ArrowUp' && e.key !== ' ' && e.key !== 'PageDown' && e.key !== 'PageUp') return;
      const i = findCurrentIndex();
      const section = sections[i];
      const rect = section.getBoundingClientRect();
      const sectionTallerThanView = rect.height > window.innerHeight;

      if (e.key === 'ArrowDown' || e.key === 'PageDown' || (e.key === ' ' && !e.shiftKey)) {
        e.preventDefault();
        if (sectionTallerThanView && !atBottomOfSection(section)) {
          const step = Math.min(window.innerHeight * STEP, rect.bottom - window.innerHeight - 20);
          if (step > 10) window.scrollBy({ top: step, behavior: 'smooth' });
          else goToSection(Math.min(i + 1, sections.length - 1));
        } else {
          goToSection(Math.min(i + 1, sections.length - 1));
        }
      } else if (e.key === 'ArrowUp' || e.key === 'PageUp' || (e.key === ' ' && e.shiftKey)) {
        e.preventDefault();
        if (sectionTallerThanView && !atTopOfSection(section)) {
          const step = Math.min(window.innerHeight * STEP, rect.top - NAV_OFFSET + 20);
          if (step > 10) window.scrollBy({ top: -step, behavior: 'smooth' });
          else goToSection(Math.max(i - 1, 0));
        } else {
          goToSection(Math.max(i - 1, 0));
        }
      }
    });
  })();
</script>
